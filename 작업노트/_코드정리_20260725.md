# 코드 단순화 후보 (2026-07-25 정찰본 · 미적용)

> ★**아직 적용하지 마라.** 적대적 검증 단계가 완료되기 전에 중단됐다.
> 이 앱에는 학생 90명 실데이터가 들어 있다. 각 항목을 검증한 뒤에만 적용할 것.
> 검증 기준: HTML 문자열 안 onclick 호출 여부 · 백업 복원 경로 관여 · 구버전 데이터 · test_regress R1.
> 적용 후 반드시 node 테스트 81건 전부 통과 확인.

## 같은 데이터를 여러 방식으로 읽는 곳 (읽기 경로 단일화)
sentOf(966~972)가 '위계 문장'의 입구를 하나로 만든 것은 성공했지만, 그 바로 옆 데이터인 **levels(학생이 고른 수준)** 는 아직 6곳에서 각자 `(ar.levels||{})[it.id]`로 직접 읽는다(890·988·993·1330·1333·1432). 같은 방어 표현이 여섯 번 복사돼 있어, sentOf가 흡수한 '형태 깨짐' 방어를 levels 쪽은 여전히 각 호출부가 스스로 지고 있다. 더 나쁜 것은 '재료가 있는가' 판정이 **두 가지 다른 의미**로 공존한다는 점이다 — dotsHTML(788)·dashCellHTML(1188)은 `Object.values(ar.levels||{}).some(v=>v)`(키가 있으면 참, 삭제된 고아 항목·문장 없는 항목도 참)이고, generate 게이트(1369)가 쓰는 hasLevels(991)는 `sentOf`가 실제 문장을 내놓는지로 본다. 같은 화면에서 '재료' 뱃지는 켜져 있는데 AI 초안 버튼은 재료 없다고 막는 상태가 구조적으로 가능하다. composedLevels(985)와 hasLevels(991)는 같은 순회를 두 번 따로 적어 둔 쌍둥이이고, '들어오는 반을 정규화해 

- **levels(고른 수준) 읽기 입구를 levelOf()로 단일화 + composedLevels/hasLevels 쌍둥이 순회 통합** [무손실확실] (884-890, 985-994, 1329-1336, 1432)
  - 문제: sentOf는 단일화됐는데 그 인자로 넘길 '수준'은 아직 6곳이 제각기 방어한다. levelSectionHTML 890 `const lv = (ar.levels||{})[it.id] || '';`, composedLevels 988 `.map(it=> sentOf(it, (ar.levels||{})[it.id]))`, hasLevels 993 `.some(it=> !!sentOf(it, (ar.levels||{})[it.id]))`, buildUserPrompt 1330 `lvItems.some(it=> (ar.levels||{}
  - 변경: 함수 3개로 정리한다(표현식 그대로 옮기기 — `ar &&` 같은 관용은 추가하지 않는다).

```js
/* 학생이 이 항목에 골라 둔 수준을 읽는 단 하나의 입구 (sentOf의 짝) */
function levelOf(ar, it){ return (ar.levels||{})[it.id]; }
/* 이 영역에서 실제로 문장이 나오는 위계 조각들 — 조합·존재판정의 공통 원천 */
function levelSents(no, ak){
  const ar = stu(no).areas[ak];
  return itemsFor(ak).m
  - 경위: 위계 문장 읽기(sentOf)만 Step 4에서 단일화 대상으로 명시됐고, 그 인자인 '수준'은 원래부터 한 줄짜리라 아무도 뽑아낼 생각을 안 한 채 복사·붙여넣기로 5곳이 더 늘었다.
- **'재료 있음' 판정의 두 갈래 중 목록·대시보드 쪽을 hasAnyLevel()로 모으기 (의미는 그대로)** [무손실확실] (788, 1188)
  - 문제: 똑같은 판정식이 두 곳에 복사돼 있다. dotsHTML 788 `const has = (ar.draft && ar.draft.trim()) || (ar.logs && ar.logs.length) || Object.values(ar.levels||{}).some(v=>v);`, dashCellHTML 1188 `const hasMat = (ar.logs && ar.logs.length) || (ar.paste||'').trim() || Object.values(ar.levels||{}).some(v=>v);`. 그런데 이 식은 gen
  - 변경: 위계 함수 근처(994행 뒤)에 이름을 붙여 두고 두 곳에서 호출한다. **식은 글자 그대로 옮긴다.**

```js
/* 목록 점·대시보드 '재료' 뱃지용 — 고른 수준 '키'가 하나라도 있는가.
   문장이 실제로 나오는지 보는 hasLevels(=AI 초안 게이트)와는 판정 기준이 다르다. */
function hasAnyLevel(ar){ return Object.values(ar.levels||{}).some(v=>v); }
```
- 788 → `... || (ar.logs && ar.logs.length) || hasA
  - 경위: dotsHTML(788)이 먼저 있었고 진행현황 대시보드를 나중에 붙이면서 판정식을 그대로 복사해 왔다. 그 사이 AI 게이트용 hasLevels가 '문장이 나오는가' 기준으로 따로 생겨 두 의미가 갈렸다.
- **외부에서 들어온 반(class)을 받아들이는 절차 3벌 → normClass/adoptClass** [무손실확실] (548-550, 1569-1571, 1714-1719)
  - 문제: '반 하나를 정규화해서 state에 꽂는다'가 세 곳에 손으로 복사돼 있다. normalizeState 548-550 `Object.values(state.classes).forEach(c=>{ if(!c.students) c.students = {}; ensureItems(c); Object.values(c.students).forEach(s=> ensureAreas(s)); });`, mergeJSON 1569-1571 `toAdd.forEach(n=>{ const c=s.classes[n]; if(!c.students) c.s
  - 변경: persistence 구역(ensureItems 아래, 573행 뒤)에 2줄을 만들고 세 곳을 치환한다.

```js
/* 반 하나를 안전한 형태로 되돌린다(멱등) — 부팅·병합·링크가 전부 이 문을 지난다 */
function normClass(c){ if(!c.students) c.students = {}; ensureItems(c); Object.values(c.students).forEach(s=> ensureAreas(s)); }
function adoptClass(n, c){ normClass(c); state.class
  - 경위: normalizeState가 먼저 있었고, 병합·링크 가져오기를 추가할 때마다 '여기서도 정규화해야 안전하다'는 판단으로 같은 세 줄을 그때그때 인라인으로 붙여 넣은 흔적이다.
- **AREAS 정의 조회 7곳 + 영역 라벨 규칙 3곳을 areaOf()/areaLabel()로** [무손실확실] (797, 806, 807, 822, 1025, 1161, 1257, 1322, 1376, 1430, 1729)
  - 문제: 영역 메타데이터를 읽는 방식이 흩어져 있다. `AREAS.find(x=>x.k===ak)`가 806·822·1025·1257·1322·1430에 그대로, 1376에는 `(AREAS.find(x=>x.k===ak)||{}).short || ''`로, 1729에는 `AREAS.find(a=>a.k===p.area) || AREAS[0]`로 변형돼 있다. 라벨 규칙도 세 벌이다 — panelHTML 797 `const lbl = a.k==='subject' ? (subjName()||'교과세특') : a.short;`, refPanel 
  - 변경: areaTitleOf(817) 바로 위에 짝을 만든다.

```js
function areaOf(ak){ return AREAS.find(x=>x.k===ak); }
function areaLabel(a){ return a.k==='subject' ? (subjName()||'교과세특') : a.short; }
```
- 806·822·1025·1257·1322·1430 → `areaOf(ak)`
- 1376 → `(areaOf(ak)||{}).short || ''` (★여기는 과목명 치환이 없는 자리다 — 그대로 `.short` 
  - 경위: AREAS가 배열이라 초기엔 find 한 줄이면 충분했고, 영역이 5개로 굳은 뒤에도 아무도 되돌아가 이름을 붙이지 않은 채 새 함수마다 같은 find를 앞줄에 적어 왔다. 라벨 3벌은 화면(panel)·사이드(ref)·현황표(dash)가 각각 다른 시기에 추가된 결과다.
- **Claude API 응답을 읽는 코드 2벌 → apiHeaders/apiDetail/apiText** [무손실확실] (1378-1402, 1483-1496)
  - 문제: generate와 genCoach가 같은 응답 데이터를 각자 파싱한다. 헤더 4개가 1380-1385(여러 줄)와 1485(한 줄)에 같은 값으로 두 번, 실패 상세 추출 `let detail=''; try{ const e=await res.json(); detail = e.error?.message || ''; }catch(_){}`가 1394와 1489에 두 번, 본문 추출 `(data.content||[]).filter(b=>b.type==='text').map(b=>b.text).join("").trim()` + 코드펜스 제
  - 변경: AI 구역 상단(SYSTEM_PROMPT 앞)에 3줄을 만들고 양쪽에서 부른다.

```js
function apiHeaders(key){ return {'content-type':'application/json','x-api-key':key,
  'anthropic-version':'2023-06-01','anthropic-dangerous-direct-browser-access':'true'}; }
async function apiDetail(res){ try{ const e=await res.json(); return e.er
  - 경위: genCoach가 generate를 복사해 만들어진 뒤 각자 조금씩 다듬어졌고, 실제로 달라진 것은 에러 문구뿐인데 파싱까지 통째로 복제된 채 남았다.
- **백업 dump 만드는 규칙 2벌 → stateDump()** [무손실확실] (1534-1535, 1587-1588)
  - 문제: state를 파일/스냅샷으로 내보내는 규칙(깊은 복사 + API 키 제거)이 두 곳에 따로 있다. exportJSON 1534-1535 `const dump = JSON.parse(JSON.stringify(state)); if(dump.settings) delete dump.settings.apiKey;`, autoBackup 1587-1588에도 같은 두 줄. 앞으로 '백업에 넣지 않을 필드'가 하나라도 늘면 한쪽만 고칠 위험이 있고, 그 사고의 결과는 파일로 유출되는 비밀값이다.
  - 변경: utils 또는 export 구역 상단에 한 줄을 만들고 두 곳을 부른다.

```js
/* 백업·스냅샷 공통 dump — API 키 등 '내보내지 않을 것'은 여기 한 곳에서만 걸러진다 */
function stateDump(){ const d = JSON.parse(JSON.stringify(state)); if(d.settings) delete d.settings.apiKey; return d; }
```
- 1534-1535 → `const dump = stateDump();`
- 1587-1588 → `const dump 
  - 경위: exportJSON이 먼저 있었고 자동백업을 나중에 붙이면서 'API 키는 빼야 한다'는 같은 판단을 두 번 내려 두 줄을 복사했다.

## 코드가 놓인 자리 · 매직값 · 전역 상태
1890줄 단일 HTML, 그중 JS는 372~1888줄이다. 전체 구조는 의외로 정연하다 — 상수 → 영속화 → 렌더 → 상호작용 → 위계 → 대시보드 → 린터 → AI → 백업 → 유틸 → 부팅 순으로 섹션 주석이 잘 유지되고 있다. 다만 "나중에 급히 필요해서 그 자리에 끼워 넣은" 흔적이 세 군데 있다: 상수 선언 블록(397~417) 한가운데에 `state`를 읽는 함수 두 개(`achvOptionsHTML`·`bankCandidates`)가 박혀 있고, 형제 정규화 함수인 `ensureItems`/`ensureAreas` 사이를 `load()`가 갈라놓고 있으며, `LV_COLORS`와 `SENTENCE_TEMPLATES` 사이를 `detectActivityType`이 끊어 놓았다. 매직값 쪽이 더 심각하다 — 기본 모델 문자열 `'claude-opus-4-8'`이 JS에만 7곳, API 엔드포인트·버전·헤더가 `generate`/`genCoach`에 통째로 두 벌, 한도 임박 기준 `0.92`가 두 곳에 각각 다른 형태로, 위계 단계 수 `10`이 14곳에 흩어져 있다. 안전벨트 문구("되돌릴 백업을 만들지 못했습니다…")도 네 곳에 복붙돼 있어

- **AI 호출 상수 4종을 한 곳으로 (모델명 7곳·엔드포인트 2곳·헤더 2벌)** [무손실확실] (535, 541, 1378-1385, 1387, 1483-1486, 1551, 1635, 1644)
  - 문제: 기본 모델 문자열 `'claude-opus-4-8'`이 JS에만 7곳에 흩어져 있다: 535 `settings:{ apiKey:"", model:"claude-opus-4-8" }`, 541 동일, 1387 `model: state.settings.model || 'claude-opus-4-8',`, 1486 동일, 1551 `state.settings={apiKey:'',model:'claude-opus-4-8'}`, 1635 동일, 1644 `document.getElementById('modelInput').value = st
  - 변경: 377-380 상수 블록(STORE_KEY·BK_KEY·SCHEMA 옆)에 추가 — ★반드시 535줄보다 위여야 한다(535가 스크립트 로드 시점에 즉시 실행되므로 아래에 두면 TDZ ReferenceError로 앱이 안 뜬다):
```js
const DEFAULT_MODEL = 'claude-opus-4-8';   // ⚠ 설정 모달 <option value>(308줄)와 같은 값을 유지할 것
const API_URL = 'https://api.anthropic.com/v1/messages';
const API_VER = '202
  - 경위: generate()를 먼저 만들고 몇 달 뒤 genCoach()를 '같은 방식으로' 복사해 붙이면서 헤더·모델·URL이 두 벌이 됐고, 복원/설정 경로는 각자 시점에 기본값을 인라인으로 적어 넣은 것으로 보인다.
- **자리가 어긋난 함수 3개 이동 (선언만 옮김, 로직 0줄 수정)** [무손실확실] (407-414, 421-427, 574-585)
  - 문제: ① 407-414 `achvOptionsHTML()`·`bankCandidates(ak)`가 상수 선언 블록 한가운데(ACHIEVEMENT와 STATES/GLYPH/IMPLBL 사이)에 끼어 있다. 특히 `bankCandidates`는 `return Object.keys(state.classes).filter(n=> n!==state.current …)`로 `state`를 읽는데, `let state`는 128줄 뒤인 535줄에 선언된다. 둘 다 호출부는 `renderItemsModal()` 안 단 한 곳씩(1028 `const c
  - 변경: 세 덩어리 모두 '잘라내기 → 붙여넣기'만 한다. 함수 선언은 호이스팅되고 전부 런타임에만 호출되므로 실행 순서·결과가 동일하다.
· `achvOptionsHTML`+`bankCandidates` → 1023 `function renderItemsModal(){` 바로 위(`/* 위계 항목 관리 모달 */` 섹션 안)
· `detectActivityType` → 526 `function templatesFor(ak, topic){` 바로 위
· `load()` → 598 `}`(ensureAreas 끝) 다음, `let _savet
  - 경위: 성취기준 앵커·반간 재사용 기능을 나중에 얹으면서 관련 상수(ACHIEVEMENT) 바로 아래가 '가까워서' 함수를 붙여 둔 것으로 보이고, load()는 스키마 잠금(_readOnly)을 넣을 때 ensureItems 바로 뒤에 새로 쓴 흔적이다.
- **'되돌릴 백업 실패 → 중단' 안전벨트를 함수 하나로 (4곳 복붙)** [무손실확실] (694-697, 1104-1107, 1546-1549, 1629-1632)
  - 문제: 파괴적 경로 4곳이 같은 패턴을 복붙하고 있고, 문구가 두 종류로 갈라져 있다. 694-697(applyRoster)·1104-1107(delItem)은 `alert('되돌릴 백업을 만들지 못했습니다(저장 공간 부족으로 보입니다).\n먼저 [⬇ 백업]으로 파일을 저장한 뒤 다시 시도하세요.');`, 1546-1549(importJSON)·1629-1632(restoreBackup)은 여기에 `\n지금 복원하면 현재 데이터를 되살릴 수 없어 중단했습니다.` 한 줄이 더 붙는다. 앞으로 문구를 다듬거나 '백업 후 재시도' 안내를 바꿀 
  - 변경: autoBackup() 정의 직후(1598줄 아래)에 추가:
```js
/* 되돌릴 지점 확보 실패 → 그 자리에서 멈춘다. 호출부는 반드시 return. */
function undoPointOk(note, restoring){
  if(autoBackup(true, note)) return true;
  alert('되돌릴 백업을 만들지 못했습니다(저장 공간 부족으로 보입니다).\n'
    + (restoring ? '지금 복원하면 현재 데이터를 되살릴 수 없어 중단했습니다.\n' : '')
    + '먼저 [⬇ 백업]으로 
  - 경위: applyRoster에 처음 안전벨트를 넣은 뒤, 항목 삭제·파일 복원·자동백업 복원을 추가할 때마다 그 블록을 복사해 붙이면서 복원 계열에만 경고 한 줄이 더 붙은 것으로 보인다.
- **복원 2경로의 'API 키 이어받기' 4줄 중복 제거** [무손실확실] (1550-1553, 1633-1636)
  - 문제: importJSON과 restoreBackup이 완전히 같은 4줄을 갖고 있다. 1550-1553: `const keepKey = state.settings ? state.settings.apiKey : ''; / state = s; if(!state.settings) state.settings={apiKey:'',model:'claude-opus-4-8'}; / if(!state.settings.apiKey && keepKey) state.settings.apiKey = keepKey;`. 1633-1636도 소스만 `JSON.p
  - 변경: restoreBackup 근처(1625줄 위)에 추가:
```js
/* 통째 교체 — 백업 파일엔 없는 API 키를 이 기기 값으로 이어받는다. */
function adoptState(next){
  const keepKey = state.settings ? state.settings.apiKey : '';
  state = next;
  if(!state.settings) state.settings = { apiKey:'', model:DEFAULT_MODEL };
  if(!state.settings.apiKey && keep
  - 경위: 파일 복원을 먼저 만들고 자동백업 복원을 나중에 추가하면서 앞의 블록을 그대로 복사한 흔적. 'API 키는 백업에 넣지 않는다' 정책이 생긴 뒤 두 곳에 각각 keepKey 처리가 붙었다.
- **한도 임박 기준 0.92 이원화 제거 — limitClass() 하나로** [무손실확실] (1185, 1263)
  - 문제: 같은 '여유/임박/초과' 판정이 서로 다른 모양으로 두 번 적혀 있다. 1185(dashCellHTML) `const cls = ch>a.limit ? 'dc-over' : ch>=a.limit*0.92 ? 'dc-warn' : 'dc-ok';`, 1263(refreshMeter) `let cls = 'ok'; if(ch > a.limit) cls='over'; else if(ch >= a.limit*0.92) cls='warn';`. 0.92라는 매직넘버가 두 곳에 박혀 있어, 임박 기준을 바꾸면 현황판과 편집기 카운터가 서로 다
  - 변경: countChars 정의 직후(1203줄 아래)에 추가:
```js
const WARN_RATIO = 0.92;   // 한도의 92%부터 '임박' 경고
function limitClass(ch, limit){ return ch>limit ? 'over' : ch>=limit*WARN_RATIO ? 'warn' : 'ok'; }
```
1185 → `const cls = 'dc-' + limitClass(ch, a.limit);`
1263 → `const cls = limitClass(ch, a.limit);`
출력 문자열('dc-
  - 경위: 편집기 카운터(refreshMeter)를 먼저 만들고, 나중에 진행 현황판을 추가하면서 같은 규칙을 CSS 클래스 접두사만 바꿔 다시 적은 것으로 보인다.
- **위계 단계 수 10 · 자동백업 보관 개수 상수화 (안내문 '7개'와 코드 '10' 불일치 노출)** [무손실확실] (560-563, 892, 969, 1053, 1063, 1091, 1120, 1127, 1594, 1778-1785, 1850)
  - 문제: 위계 1~10의 '10'이 14곳에 리터럴로 박혀 있다 — 560 `it.sentences = Array(10).fill('')`, 562 `it.sentences.slice(0,10)`, 563 `while(it.sentences.length < 10)`, 892 `Array.from({length:10},(_,i)=>i+1)`, 969 `if(!(n>=1 && n<=10)) return '';`, 1053 `Array.from({length:10},(_,lv)=>`, 1063·1091·1120·1127·1778·1782 `Arr
  - 변경: AREAS 선언 근처(381줄 위)에 `const LV_MAX = 10;   // 위계 단계 수(1~LV_MAX) — sentences 배열 길이와 동일`, autoBackup 위(1580줄)에 `const AUTOBK_KEEP = 10;  // ⚠ 안내문(353줄)은 '7개'로 적혀 있음 — 어느 쪽이 정본인지 결정 필요`를 두고, 위 목록의 리터럴만 치환한다(`Array(LV_MAX).fill('')`, `slice(0,LV_MAX)`, `{length:LV_MAX}`, `n>=1 && n<=LV_MAX`, `> AUTOBK_K
  - 경위: 과세특 도구에서 위계 1~10 템플릿을 이식할 때 각 지점마다 배열 길이를 손으로 적었고, 자동백업은 처음 7개로 만들었다가 스키마 전환 pin 기능을 넣으며 10으로 늘리면서 안내 문구를 함께 못 고친 것으로 보인다.

## 중복 코드 — 같은 규칙이 두 벌 이상 손으로 타이핑된 지점
index.html은 1890줄 단일 파일이고, 이 관점에서 볼 때 '알고리즘 중복'보다 **같은 규칙을 손으로 두 번 적어 서로 미세하게 어긋난 곳**이 문제다. 가장 큰 덩어리는 Claude API 호출로, generate(1378~1403)와 genCoach(1483~1496)가 엔드포인트·헤더·상태코드 처리·코드펜스 제거까지 40줄 가까이 같은 일을 하는데 이미 갈라져 있다(코치 쪽 네트워크 판정 정규식에 NetworkError가 빠져 있고 401/429 문구도 다르다). 백업 실패 가드 문구는 5곳에 복붙돼 있고 그중 1760줄만 '스냅샷/저장공간'으로 표기가 달라진 상태다. 기본 모델 문자열 'claude-opus-4-8'은 JS에 7곳(+HTML 옵션 1곳) 하드코딩돼 있어 모델을 바꾸는 날 한 곳만 빠뜨리기 딱 좋다. 그 밖에 '전체 상태 교체 + API키 보존' 5줄이 2벌(1550·1633), 'API키 제거 후 덤프' 2줄이 2벌(1534·1587), Blob 다운로드 3줄이 2벌(1537·1622), '반 정규화 트리오(students→ensureItems→ensureAreas)'가 3벌(548·1569·1714)이다. 반면 프롬프트가 

- **Claude API 호출 2벌(generate·genCoach) → claudeText() 하나로** [무손실확실] (1378-1403 (generate) · 1483-1496 (genCoach))
  - 문제: 두 함수가 같은 호출을 각자 타이핑했다. generate: `const res = await fetch('https://api.anthropic.com/v1/messages', { method:'POST', headers:{ 'content-type':'application/json', 'x-api-key': key, 'anthropic-version':'2023-06-01', 'anthropic-dangerous-direct-browser-access':'true' }, ...` / genCoach(1485): 같은 헤더를 한 줄
  - 변경: 공용 헬퍼를 SYSTEM_PROMPT 앞(1289 근처)에 추가:

```js
/* Claude 호출 단일 입구 — 엔드포인트·헤더·응답 파싱은 여기에만 존재.
   상태코드별 문구는 호출부(mapErr)가 정하므로 기존 메시지가 그대로 유지된다. */
async function claudeText(key, maxTokens, system, user, mapErr){
  const res = await fetch('https://api.anthropic.com/v1/messages', {
    method:'POST',
    h
  - 경위: genCoach가 나중에 붙으면서 generate를 눈으로 보고 옮겨 적었고, 그 과정에서 헤더를 한 줄로 압축하고 429 문구를 줄이고 NetworkError 판정을 흘렸다.
- **'되돌릴 백업 실패 → 중단' 가드 4벌 → ensureUndoPoint()** [무손실확실] (694-698 · 1104-1107 · 1546-1549 · 1629-1632 (참고: 1758-1761은 제외))
  - 문제: 위험 작업 직전 스냅샷을 만들고 실패하면 중단하는 5줄짜리 패턴이 네 군데 복붙돼 있고, 경고문 전문까지 그대로 타이핑돼 있다. 695·1105는 `alert('되돌릴 백업을 만들지 못했습니다(저장 공간 부족으로 보입니다).\n먼저 [⬇ 백업]으로 파일을 저장한 뒤 다시 시도하세요.');`, 1547·1630은 같은 문장에 `\n지금 복원하면 현재 데이터를 되살릴 수 없어 중단했습니다.`만 끼운 판본. 다섯 번째 사본인 1760은 이미 갈라져 `'되돌릴 스냅샷을 만들지 못했습니다(저장공간 부족으로 보입니다)…'`로 표기가 다르다(스
  - 변경: autoBackup 아래(1598 뒤)에 추가:

```js
/* 되돌릴 지점을 먼저 만든다. 실패하면 false — 호출부는 즉시 중단해야 한다. */
const UNDO_BLOCK = '지금 복원하면 현재 데이터를 되살릴 수 없어 중단했습니다.\n';
function ensureUndoPoint(note, extra){
  if(autoBackup(true, note)) return true;
  alert('되돌릴 백업을 만들지 못했습니다(저장 공간 부족으로 보입니다).\n'
    + (extra||'') + '먼저 [⬇ 백
  - 경위: 기능이 하나씩 늘 때마다(명단 재적용 → 항목 삭제 → 파일 복원 → 자동백업 복원 → 기록 병합) 앞의 블록을 복사해 붙였고, 마지막 사본에서 문구가 갈라졌다.
- **기본 모델 문자열 7곳 + 기본 settings 객체 4곳 상수화** [무손실확실] (535 · 541 · 1387 · 1486 · 1551 · 1635 · 1644)
  - 문제: `'claude-opus-4-8'`이 JS에만 7곳 하드코딩돼 있다. 535 `let state = { current:null, classes:{}, settings:{ apiKey:"", model:"claude-opus-4-8" } };`, 541 `if(!state.settings) state.settings = { apiKey:"", model:"claude-opus-4-8" };`, 1387 `model: state.settings.model || 'claude-opus-4-8',`, 1486(같은 값 다시), 1551·1
  - 변경: `const SCHEMA = 1;`(379) 바로 아래에 선언(535보다 물리적으로 앞이어야 한다 — 최상위 실행 순서):

```js
const DEFAULT_MODEL = 'claude-opus-4-8';   // ⚠ 설정 모달 <option> 목록(HTML 307-311)과 함께 유지할 것
function defaultSettings(){ return { apiKey:"", model:DEFAULT_MODEL }; }   // ★반드시 함수 — 공용 객체를 돌려주면 복원 때 state끼리 같은 객체를 공유해 버린다
```
치환
  - 경위: 모델 선택 기능이 나중에 붙으면서 '기본값 폴백'을 필요할 때마다 그 자리에 적었다. 아직 7곳 값이 전부 같아 사고는 안 났지만, 다음 모델 교체가 첫 어긋남 지점이 된다.
- **'API키 빼고 덤프' 2벌 + Blob 다운로드 3줄 2벌 → dumpState()/downloadJSON()** [무손실확실] (1533-1539 (exportJSON) · 1587-1588 (autoBackup) · 1619-1624 (downloadBackup))
  - 문제: 개인정보 규칙이 두 곳에 각각 적혀 있다. exportJSON 1534-1535 `const dump = JSON.parse(JSON.stringify(state)); if(dump.settings) delete dump.settings.apiKey;   // API 키는 백업 파일에 포함하지 않음` 과 autoBackup 1587-1588 `const dump = JSON.parse(JSON.stringify(state)); if(dump.settings) delete dump.settings.apiKey;             
  - 변경: exportJSON 자리에 정본 3개를 둔다:

```js
/* 밖으로 나가는 사본 — API 키는 절대 포함하지 않는다(파일 백업·자동 백업 공통 규칙). */
function dumpState(){
  const dump = JSON.parse(JSON.stringify(state));
  if(dump.settings) delete dump.settings.apiKey;
  return dump;
}
function downloadJSON(obj, filename){
  const blob = new Blob([JSON.str
  - 경위: exportJSON이 먼저 있었고 자동백업이 나중에 붙으면서 '깊은복사 → 키 제거'와 'Blob → a.click()'을 그대로 복사해 왔다. 주석 문구만 그때그때 다르게 달렸다.
- **전체 상태 교체(파일 복원·자동백업 복원) 2벌 → adoptState()** [무손실확실] (1550-1553 (importJSON) · 1633-1637 (restoreBackup))
  - 문제: '이 기기의 API 키는 살리고 나머지를 통째로 갈아끼운다'는 규칙이 두 벌이다. importJSON 1550-1553: `const keepKey = state.settings ? state.settings.apiKey : ''; state = s; if(!state.settings) state.settings={apiKey:'',model:'claude-opus-4-8'}; if(!state.settings.apiKey && keepKey) state.settings.apiKey = keepKey; normalizeState()
  - 변경: restoreBackup 앞에 정본을 둔다:

```js
/* 전체 상태 교체 단일 경로 — 백업 파일엔 키가 없으므로 이 기기 키를 되살려 준다. */
function adoptState(next){
  const keepKey = state.settings ? state.settings.apiKey : '';
  state = next;
  if(!state.settings) state.settings = defaultSettings();
  if(!state.settings.apiKey && keepKey) state.sett
  - 경위: 파일 복원이 먼저 있었고 자동백업 복원 기능이 나중에 추가되면서 그 블록을 복사한 뒤 소스만 b.data로 갈아 끼웠다.
- **반(class) 정규화 트리오 3벌 → normalizeClass()** [무손실확실] (548-550 (normalizeState) · 1569-1571 (mergeJSON) · 1714-1719 (handleHashImport))
  - 문제: '학생칸 보장 → ensureItems → 학생마다 ensureAreas' 3줄이 세 군데에 각각 적혀 있다. 548-550 `Object.values(state.classes).forEach(c=>{ if(!c.students) c.students = {}; ensureItems(c); Object.values(c.students).forEach(s=> ensureAreas(s)); });`, 1569-1571 `toAdd.forEach(n=>{ const c=s.classes[n]; if(!c.students) c.student
  - 변경: ensureItems 아래(573 뒤)에 정본:

```js
/* 반 하나를 안전한 형태로 되돌린다(멱등). 밖에서 들어온 반은 전부 이 문을 통과시킨다. */
function normalizeClass(c){
  if(!c.students) c.students = {};
  ensureItems(c);
  Object.values(c.students).forEach(s=> ensureAreas(s));
  return c;
}
```
- normalizeState 548-550 → `Object.values(state.classe
  - 경위: normalizeState가 원본이고, 병합 복원과 #import 링크 가져오기가 차례로 추가될 때마다 그 루프 본문을 복사해 붙였다.

## CSS `<style>` 블록 · HTML 마크업 · 다크모드 토큰 일관성
index.html의 CSS는 221줄(11~232)에 121개 규칙이 들어 있고, 구조 자체는 놀랍도록 건강하다 — 다크모드가 "토큰만 오버라이드, 컴포넌트 CSS 불변" 원칙을 실제로 지키고 있고, 중복 선언된 셀렉터(같은 셀렉터가 두 번 나오는 경우)는 단 한 건도 없다. 꼬임은 두 종류에 몰려 있다. 첫째는 **화면 구조가 바뀌었는데 CSS만 남은 것**: 2-pane 마스터-디테일로 갈아엎으면서 아코디언 렌더러는 지웠지만(792줄 주석이 그 사실을 명시) `.card`/`.chead`/`.chev`/`.cbody`/`.card.open` 6줄이 통째로 남아 있다. 둘째는 **같은 값을 가리키는 통로가 둘인 것**: `--white`와 `--surface`는 라이트(#fff)에서도 다크(#182633)에서도 값이 완전히 동일한데 이름만 둘이라, 앞으로 한쪽만 손대면 소리 없이 어긋난다. 하드코딩 hex는 총 41개인데 이 중 `#fff` 8곳은 "색 배경 위 흰 글자"로 다크에서도 흰색이어야 하므로 리터럴이 **옳고**, 반대로 `#cfe0f0`(3곳)·`#7a8794`(3곳)·`#fdeccf`+`#9a5a12`(2곳)는 순수 중복이라 :root 전용

- **2-pane 전환 때 남겨진 아코디언 카드 CSS 6줄 삭제 (완전 사문화)** [무손실확실] (74-75, 81-84 (76-80은 유지))
  - 문제: 792줄에 `/* (구 아코디언 카드 렌더러 제거 — master-detail rowHTML/renderDetail로 대체) */` 라는 주석이 있는데, 그 렌더러가 쓰던 CSS는 그대로 남아 있다. 파일 전체(HTML + JS 템플릿 문자열 포함)를 grep한 결과 `card`·`chead`·`chev`·`cbody`·`open` 다섯 클래스는 <style> 블록 밖에서 **단 한 번도 등장하지 않는다**. (`ref-card`는 별개 클래스, `openNo`/`openArea`/`openItems`는 변수·함수명이라 `\bope
  - 변경: 74·75 두 줄과 81~84 네 줄을 삭제. ★76~80(`.cno` `.cname` `.cdots` `.cdot` `.cdot.has`)은 rowHTML(730-731)·detail-head(752-753,763-764)·dotsHTML(787-789)에서 살아 있으므로 **반드시 남긴다** — 특히 `.cdot`/`.cdots`는 R1이 대조하는 dotsHTML 출력에 들어간다.
삭제 후 73줄 섹션 주석은 `/* ---- 학생 행 번호·이름·진행 점 ---- */` 정도로 고치면 남은 5줄의 정체가 분명해진다(주석이라 렌더링
  - 경위: 아코디언 카드 → 마스터-디테일 2-pane으로 UI를 갈아엎을 때 JS 렌더러는 지우고 주석까지 남겼는데, CSS는 '혹시 몰라' 놔둔 뒤 잊힌 전형적 패턴.
- **--white 와 --surface 는 라이트·다크 양쪽에서 값이 100% 같은 쌍둥이 토큰 — 하나로 통합** [무손실확실] (17, 26 (정의) / 53, 74, 192, 195, 207 (사용))
  - 문제: 두 토큰이 두 테마 모두에서 **완전히 같은 값**이다.
  17 `--bg:#EAF1F8; --white:#fff;`  ·  20 `--surface:#fff; --soft:#f6fafd; ...`  → 라이트에서 둘 다 #fff
  26 `--bg:#0e1720; --white:#182633; --surface:#182633; --soft:#14202c; --soft2:#1e2f3e;` → 다크에서 둘 다 #182633
즉 지금은 구별이 아니라 **읽기 경로가 둘로 갈라진 상태**다. `var(--white)`는 5곳(53 .t
  - 변경: 중립적 이름인 `--surface`로 단일화한다.
① 17줄: `--bg:#EAF1F8; --white:#fff;` → `--bg:#EAF1F8;`  (`--white` 정의 **삭제**)
② 26줄: `--bg:#0e1720; --white:#182633; --surface:#182633; ...` → `--bg:#0e1720; --surface:#182633; ...`  (`--white:#182633;` **삭제**)
③ 53·192·195·207 네 곳의 `var(--white)` → `var(--surface)`  (74줄
  - 경위: 양식 B 팔레트에서 그대로 가져온 레거시 이름(--white)이 먼저 있었고, 나중에 다크모드를 붙이면서 '중립 표면' 개념의 --surface를 새로 만들었는데, 기존 --white 사용처를 정리하지 않은 채 값만 다크값으로 맞춰 놓아 쌍둥이가 됐다(19줄 주석 '중립 표면·테두리 토큰(다크모드 전환용)'이 그 시점을 증언한다).
- **중복된 하드코딩 hex 8곳 → :root 전용 토큰 4개 (다크 블록은 손대지 않아 픽셀 동일)** [무손실확실] (66-67, 71, 113, 120, 134, 141, 176 (+ :root 20-22에 정의 추가))
  - 문제: 같은 색 리터럴이 여러 규칙에 복붙돼 있어 한 곳만 고치면 어긋난다.
  #cfe0f0 ×3 — 67 `.btn.ghost{...border:1.5px solid #cfe0f0;}` / 71 `.iconbtn{...border:1.5px solid #cfe0f0;...}` / 141 `.spin{...border:2px solid #cfe0f0;...}`
  #7a8794 ×3 — 66 `.btn.gray{background:#7a8794;}` / 113 `.seg button.on.b{background:#7a8794;color:
  - 변경: :root(20~22줄 근처)에 **새 이름**으로 4개 추가하고, 사용처만 치환한다.
  `--ghost-brd:#cfe0f0;   /* ghost 버튼·아이콘 버튼·스피너 테두리 */`
  `--gray:#7a8794;        /* 중립 회색 — 취소 버튼 / 인상 '아쉬움' 표식 */`
  `--warn-bg:#fdeccf; --warn-ink:#9a5a12;   /* 주의 칩 · 현황표 '재료' 배지 */`
치환: 67·71·141의 `#cfe0f0` → `var(--ghost-brd)` / 66·113·120의 `#7
  - 경위: 버튼·칩 컴포넌트를 필요할 때마다 하나씩 붙여 나가면서 그때그때 직전 규칙에서 색을 복사해 왔고, 다크모드 토큰화는 나중에 --surface/--brd 계열 '구조색'만 훑고 지나가 '장식색'은 리터럴로 남았다.
- **똑같은 표면 선언이 세 벌 복붙 — 그룹 셀렉터로 단일화 (총 13개 중복 선언 제거)** [무손실확실] (67·71 / 195·207 / 105·152·187·218)
  - 문제: 세 세트가 문자 그대로 같은 선언을 반복한다.
(A) ghost 외형 — 67 `.btn.ghost{background:var(--soft2);color:var(--blue-dark);border:1.5px solid #cfe0f0;}` 와 71 `.iconbtn{background:var(--soft2);color:var(--blue-dark);border:1.5px solid #cfe0f0;border-radius:9px;min-height:40px;padding:8px 11px;font-size:13px;cursor:point
  - 변경: 공통분만 그룹 셀렉터로 올리고 개별 규칙은 나머지만 남긴다. 세 세트 모두 단일 클래스 셀렉터끼리라 그룹화해도 명시도가 각자 그대로 유지되고(그룹 셀렉터는 각 셀렉터마다 명시도를 따로 계산), 이 6개 클래스의 background/border/box-shadow를 덮어쓰는 다른 규칙이 파일 안에 없음을 전수 확인했다.
(A) 67줄을 `.btn.ghost,.iconbtn{background:var(--soft2);color:var(--blue-dark);border:1.5px solid var(--ghost-brd);}` 로 만들고
  - 경위: 컴포넌트를 하나씩 추가할 때마다 바로 위 규칙을 복사해 붙이고 radius/padding만 눈대중으로 조정한 흔적. 9/10/11px 세 값이 나란히 있는 게 결정이 아니라 복붙의 부산물임을 보여 준다.
- **결과가 0인 규칙·미사용 토큰·중복 @media 블록 정리** [무손실추정] (35 / 13·16·28·29 / 100·107·126 / 45-48·211 / 215·217)
  - 문제: ① 35줄 `html[data-theme="dark"] body{background:var(--bg);}` 는 **아무 일도 하지 않는다.** 37줄 `html,body{...background:var(--bg);...}` 가 이미 body에 같은 선언을 걸고, `--bg` 는 26줄에서 테마별로 재정의되므로 body의 계산값은 두 경우 모두 `var(--bg)` 로 동일하다. 명시도만 높고 값이 같은 순수 no-op이다.
② `--green-pastel`(13, 29줄)과 `--line`(16, 28줄)은 라이트·다크 양쪽에 정의
  - 변경: ① 35줄 삭제.
② 13줄에서 ` --green-pastel:#C3E9CC;`, 16줄에서 ` --line:#BBBBBB;`, 28줄에서 `--line:#33475b;`, 29줄에서 ` --green-pastel:#1f4030;` 제거(각 줄의 나머지 토큰은 유지).
③ 100·107·126의 `font-size:16px;` 제거. — 단 이 셋은 iOS 자동 확대 방지용 방어적 중복일 수 있으니, 57줄에 `/* 16px 미만이면 iOS가 입력 시 화면을 확대함 — 단일 출처 */` 주석을 남기는 조건에서만 지울 것. 지우기 싫
  - 경위: ①은 다크모드를 붙일 때 body 배경이 안 바뀔까 봐 넣은 보험인데, 토큰 오버라이드 방식으로 정리한 뒤 지우는 걸 잊은 것. ②는 학습지 양식 B 팔레트를 통째로 복사해 온 잔재. ④는 `.detail-head .back` / `.editor-ref` 를 나중에 추가하면서 기존 미디어 블록까지 올라가 찾기 귀찮아 그 자리에 새로 연 것.
- **[손대지 말 것] 다크모드 하드코딩 잔재와 스코프 누락 — 전부 외형이 바뀌므로 리팩터링 대상 아님** [risky] (79, 109, 121, 133-136, 176-177, 192, 224-226 / 97·1609 / 307 / 116·160 / 843 / 1171)
  - 문제: 아래는 '고치면 나아 보이지만 반드시 렌더링이 바뀌는' 것들이라 순수 리팩터링으로 분류할 수 없다. 판단 근거로만 남긴다.
(a) 다크에서 라이트 색이 그대로 남는 규칙 13개: 79 `.cdot{background:#e2e8ef}`, 109 `.seg button{color:#b3bfca}`, 121 `.xbtn{color:#c0392b}`, 133 `.chip.bad{background:#fbe4e1}`, 135 `.chip.tip{background:#eaf3ff}`, 136 `.chip.good{background:#e3f5
  - 변경: 모두 **변경하지 않는다.** (a)·(d)·(e)는 사용자가 다크모드 가독성을 개선하기로 별도 결정했을 때 처리할 목록으로만 남긴다. 제안 3이 (a)의 일부를 토큰화해 두면 그때 `html[data-theme="dark"]` 블록에 값 몇 줄만 얹는 작업으로 끝난다는 점만 참고.
  - 경위: 다크모드를 '토큰만 오버라이드' 원칙으로 나중에 얹으면서 --surface/--brd 같은 구조색은 훑었지만 칩·배지·스피너 같은 장식색은 손대지 않았고, `.hint`·`select.txt` 는 컴포넌트를 나중에 재사용하며 원래 스코프를 넘어간 경우.

## 더 이상 호출되지 않는 것 — 죽은 함수·도달 불가 분기·구버전 잔재·미사용 CSS
index.html(1890줄)의 함수 118개 전부에 대해 정의 1건 + 호출 ≥1건을 확인했다. onclick 등 HTML 문자열 안 호출까지 포함해 세었고, `on(click|input|change|blur)="X("` 로 잡히는 이름이 전부 실제 정의와 짝을 이루는지도 역방향으로 검증했다 — **완전히 죽은 JS 함수는 하나도 없다.** 전역 변수·상수 27개, 정적 element id 29개도 모두 소비처가 있다. 주석 처리된 코드 블록은 0건(모든 주석이 설명문), 중복 정의된 함수·CSS 셀렉터도 0건이다. 즉 JS 쪽은 이미 매우 깨끗하다. 반면 **CSS에는 폐기된 아코디언 UI의 잔해가 통째로 남아 있다** — `.card`/`.chead`/`.chev`/`.cbody`와 `.card.open ...` 6줄인데, `classList.add/remove/toggle` 호출 전수조사 결과 `card`·`open` 클래스를 붙이는 코드가 앱 어디에도 없어 100% 도달 불가다(792줄의 "구 아코디언 카드 렌더러 제거" 묘비 주석이 이를 뒷받침하며, v3.2 박제본에도 이미 죽은 채로 들어 있었다). 그 밖에 `.btn.block`, CSS 토큰

- **폐기된 아코디언 카드 UI의 CSS 잔해 6줄 삭제** [무손실확실] (74-75, 81-84 (블록 헤더 73줄 `/* ---- student cards ---- */` 는 유지))
  - 문제: 792줄에 `/* (구 아코디언 카드 렌더러 제거 — master-detail rowHTML/renderDetail로 대체) */` 묘비 주석이 있는데, 그 UI가 쓰던 CSS는 그대로 남아 있다.
```
74  .card{background:var(--white);border-radius:13px;box-shadow:0 1px 3px rgba(0,0,0,.06);margin-bottom:9px;overflow:hidden;}
75  .chead{display:flex;align-items:center;gap:10px;padding
  - 변경: 74·75·81·82·83·84줄을 삭제. 같은 블록의 `.cno`(76) `.cname`(77) `.cdots`(78) `.cdot`(79) `.cdot.has`(80)는 rowHTML·dotsHTML·detail-head가 지금도 쓰므로 **반드시 남긴다**. 결과:
```css
/* ---- student rows / dots ---- */
.cno{...}
.cname{...}
.cdots{...}
.cdot{...}
.cdot.has{...}
```
  - 경위: 아코디언(카드 펼침) UI → master-detail 2-pane으로 전환할 때 JS 렌더러(cardHTML/toggleOpen)만 지우고 스타일시트는 손대지 않았다. v3.2 박제본에도 이미 죽은 상태로 들어 있어, 최소 두 버전을 넘게 방치된 잔재다.
- **소비처 없는 CSS 규칙 1줄 + 미사용 디자인 토큰 2개** [무손실확실] (69, 13, 16, 28, 29)
  - 문제: `.btn.block`(69줄 `.btn.block{width:100%;}`)은 `block` 이라는 문자열이 파일 안에 7번 나오지만 나머지 6번은 전부 `display:block` / `display:inline-block` / `scrollIntoView({block:'nearest'})` 이고, **클래스 토큰으로 쓰이는 곳은 0곳**이다.
토큰 `--green-pastel`은 13줄(`--blue-pastel:#C5E3F7; --green-pastel:#C3E9CC;`)과 29줄(다크 오버라이드)에 선언만 있고 `var(-
  - 변경: ① 69줄 `.btn.block{width:100%;}` 삭제.
② 13줄 → `  --blue-pastel:#C5E3F7;` / 29줄 → `  --blue-pastel:#1e3a5c;` (각 줄에서 `--green-pastel:...;` 만 제거)
③ 16줄·28줄에서 `--line:#BBBBBB;` / `--line:#33475b;` 만 제거
※ 참고(같은 성격이지만 이번엔 손대지 말 것): 801줄 `class="editor-main"`, 1052줄 `class="itembody hidden"` 은 반대로 **CSS 규칙이 
  - 경위: `--green-pastel`·`--line`은 학습지 양식 B의 표준 토큰 팔레트를 통째로 복사해 오면서 따라온 것이고(CLAUDE.md의 색상 토큰 목록과 동일), 이 앱은 그중 일부만 쓴다. `.btn.block`은 모바일 전폭 버튼용으로 만들어 뒀다가 `.genrow .btn{flex:1}` + 480px 미디어쿼리 방식으로 대체된 흔적.
- **handleHashImport의 반별 정규화 — 바로 뒤 normalizeState()가 그대로 다시 함** [무손실추정] (1714-1719 (직후 1721줄과 함께 봐야 함))
  - 문제: 링크 가져오기 경로만 정규화를 두 번 돈다.
```js
1714  target.forEach(n=>{
1715    const c = incoming.classes[n]; if(!c.students) c.students = {};
1716    ensureItems(c);
1717    Object.values(c.students).forEach(st=>ensureAreas(st));
1718    state.classes[n] = c;
1719  });
1721    if(incoming.current && ...) ...; no
  - 변경: 1716·1717 두 줄만 제거하고 1715의 `if(!c.students) c.students = {};` 는 남긴다(이게 `c`가 null인 불량 페이로드를 대입 전에 막는 방벽 역할도 하므로).
```js
target.forEach(n=>{
  const c = incoming.classes[n]; if(!c.students) c.students = {};
  state.classes[n] = c;
});
```
※ `ensureItems`/`ensureAreas` 루프를 통째로 걷어내고 1715까지 지우면 안 된다 — `{"
  - 경위: #import= 링크 경로를 mergeJSON에서 복사해 만든 뒤, 나중에 `state.current` 처리 때문에 `normalizeState()`를 추가하면서 원본에서 온 정규화 루프를 지우지 않았다.
- **importRecords의 도달 불가 분기 — tnoOf는 null이 될 수 없다** [무손실추정] (1836-1837)
  - 문제: ```js
1836  const tnoOf = Object.keys(c.students).find(k=> c.students[k] === st);
1837  if(tnoOf == null){ missing.push((no + ' ' + (inc.name||'')).trim()); return; }   // 정상 학생이 아님
```
이 지점에 도달할 때 `st`는 반드시 truthy이며(1829줄 `if(!st){ ... return; }` 통과), `st`가 대입되는 경로는 1808줄 `c.students[no]`(hasOwnPro
  - 변경: 1837줄만 삭제하고 1836은 유지(바로 아래 1839-1842의 `usedTno[tnoOf]` 충돌 감지에 필요).
```js
const tnoOf = Object.keys(c.students).find(k=> c.students[k] === st);
const who = nameBase(inc.name);
```
  - 경위: 1839-1842의 '한 자리에 두 사람이 배정되는' 오배정 차단 로직을 나중에 덧붙이면서, tnoOf 계산에 대한 방어 가드를 반사적으로 같이 붙였다. 실제로는 st의 출처가 c.students로 한정돼 있어 필요 없는 가드가 됐다.
- **bindPanel — 한 줄짜리 우회 함수** [무손실확실] (757, 927)
  - 문제: ```js
757    requestAnimationFrame(()=>{ bindPanel(openNo); });
927  function bindPanel(no){ refreshMeter(no, openArea); }
```
`bindPanel`은 파일 전체에서 정의 1회 + 호출 1회뿐이고(onclick 문자열 포함 전수 확인), 하는 일은 `refreshMeter` 한 번 부르는 것뿐이다. _test/harness.js의 NAMES 노출 목록에도 없어 테스트가 이 이름에 의존하지 않는다.
  - 변경: 927줄 삭제하고 757줄을 직접 호출로:
```js
requestAnimationFrame(()=>{ refreshMeter(openNo, openArea); });
```
(`bindPanel(openNo)` → 내부에서 `refreshMeter(openNo, openArea)` 였으므로 인자까지 동일)
  - 경위: 아코디언 시절에는 카드를 열 때 미터·세그먼트·리스너를 한꺼번에 붙이는 '패널 바인딩' 단계가 있었는데, master-detail로 옮기며 나머지가 다 사라지고 refreshMeter 한 줄만 남은 껍데기다.
- **[risky · 삭제 금지] ev / pre / grp — 읽히지 않지만 지우면 안 되는 필드** [risky] (593-594, 1112, 1779)
  - 문제: 정적 분석만 하면 죽은 코드로 보인다.
```js
593  if(!s.areas[a.k].ev  || typeof s.areas[a.k].ev !=='object') s.areas[a.k].ev  = {};
594  if(!s.areas[a.k].pre || typeof s.areas[a.k].pre!=='object') s.areas[a.k].pre = {};
1112   if(ar && ar.pre) delete ar.pre[id];
1779   if(rb.grp) it.grp = String(rb.grp);
```
`ev`는 
  - 변경: **변경하지 않는다.** 이유 셋: ① `_설계/spec_wigye_v33.md`가 `ev`(활동별 개별 장면)·`pre`(일괄 채움 미확인 값)를 v3.3 위계 Step의 데이터 스키마로 규정하고 있고 592줄 주석도 그 의도를 명시한다. ② `grp`는 `_설계/spec_rubric배관.md`의 배관 규격이며 `_test/test_records.js:162,172`가 실제로 이 필드를 단언한다 — 지우면 테스트 81건 중 일부가 즉시 깨진다. ③ `ev`·`pre` 초기화를 없애면 localStorage에 저장되는 area 객체
  - 경위: 진행 중인 v3.3(위계 Step) 작업의 전방 호환 스캐폴딩을 미리 심어 둔 것이다. '구버전이 모르는 키라 백업 왕복에도 살아남는다'는 592줄 주석이 목적을 명시하고 있으며, 지금 안 읽히는 것은 기능이 아직 안 붙었기 때문이지 폐기됐기 때문이 아니다.
