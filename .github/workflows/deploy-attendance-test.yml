name: Deploy attendance-test

on:
  push:
    branches: [master]
    paths:
      - '교사업무/attendance-test/**'
      - '.github/workflows/deploy-attendance-test.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Prepare deploy files
        run: |
          mkdir -p deploy-tmp
          cp 교사업무/attendance-test/index.html    deploy-tmp/
          cp 교사업무/attendance-test/manifest.json deploy-tmp/
          cp 교사업무/attendance-test/sw.js         deploy-tmp/
          cp 교사업무/attendance-test/icon.svg      deploy-tmp/
          [ -f "교사업무/attendance-test/xlsx.full.min.js"   ] && cp 교사업무/attendance-test/xlsx.full.min.js   deploy-tmp/
          [ -f "교사업무/attendance-test/pdf.min.js"          ] && cp 교사업무/attendance-test/pdf.min.js          deploy-tmp/
          [ -f "교사업무/attendance-test/pdf.worker.min.js"   ] && cp 교사업무/attendance-test/pdf.worker.min.js   deploy-tmp/
          [ -d "교사업무/attendance-test/cmaps"               ] && cp -r 교사업무/attendance-test/cmaps            deploy-tmp/

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: ./deploy-tmp
