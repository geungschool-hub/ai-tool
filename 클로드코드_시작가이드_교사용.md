# 클로드 코드(Claude Code) 시작 가이드 — 선생님용

> 코딩 경험이 없어도 됩니다. **"컴퓨터한테 말로 시키는 것"** 이 전부입니다.
> 이 문서는 설치 → 첫 실행 → 실제 업무에 써먹기까지 30분 안에 끝내는 것을 목표로 합니다.

---

## 0. 이게 대체 뭔가요

챗GPT나 클로드 웹사이트는 **대화만** 합니다. 답을 받아서 내가 복사해 붙여넣어야 하죠.

클로드 코드는 **내 컴퓨터의 폴더를 직접 열어보고, 파일을 만들고 고칩니다.**

| 이런 걸 시킬 수 있습니다 | 결과물 |
|---|---|
| "이 PDF 교과서에서 그림 뽑아서 학습지 만들어줘" | 인쇄 가능한 HTML 학습지 + 정답지 |
| "수행평가 채점표를 학생별로 정리해줘" | 엑셀/표 파일 |
| "학생들이 폰으로 제출하는 설문 페이지 만들어줘" | 실제 작동하는 웹앱 |
| "이 hwp 파일들 내용 전부 뽑아서 한 파일로 합쳐줘" | 정리된 텍스트 |
| "학급 임원 선거 투표 앱 만들어줘" | 학생 폰 기표 + 전자칠판 개표 화면 |

핵심은 **"결과물 파일이 내 폴더에 실제로 생긴다"** 는 것입니다.

---

## 1. 준비물

### 계정 (이게 제일 중요)
- **무료 클로드 계정으로는 안 됩니다.** Pro 이상 유료 구독이 필요합니다.
- 요금제 확인: https://claude.com/pricing
- 개인 카드로 결제하고 개인 계정으로 쓰는 구조입니다. 학교 예산·공용 계정으로 쓰려면 별도 협의가 필요합니다.

### 컴퓨터
- 윈도우 10(1809 이상) / 윈도우 11 / 맥 / 리눅스
- 램 4GB 이상, 인터넷 연결
- **학교 업무용 PC는 보안 프로그램 때문에 설치가 막힐 수 있습니다.** 개인 노트북으로 먼저 시도해 보시길 권합니다.

### 그 외 프로그램은?
파이썬·깃허브 같은 건 **지금 필요 없습니다.** 하고 싶은 일이 늘어나면 그때 깔면 됩니다 → [5장](#5-곁들여-필요한-프로그램--계정)

---

## 2. 설치 — 두 가지 길

### 길 A. 데스크톱 앱 (터미널이 무섭다면 이쪽) ★ 추천

검은 화면 명령어 없이, 일반 프로그램처럼 설치해서 씁니다.

1. https://claude.com/download 접속
2. 윈도우용 설치 파일 다운로드 → 실행
3. 앱을 켜고 클로드 계정으로 로그인
4. 작업할 **폴더를 선택**하고 대화 시작

맥은 같은 페이지에서 macOS용을 받으시면 됩니다.

### 길 B. 터미널 설치 (나중에 더 자유롭게 쓰려면)

**윈도우:**
1. 시작 버튼 옆 검색창에 `PowerShell` 입력 → **Windows PowerShell** 실행
   (관리자 권한 필요 없습니다)
2. 아래 한 줄을 그대로 복사해 붙여넣고 엔터:

```powershell
irm https://claude.ai/install.ps1 | iex
```

**맥 / 리눅스:** 터미널을 열고

```bash
curl -fsSL https://claude.ai/install.sh | bash
```

**설치 확인** — 터미널을 껐다 켠 뒤:

```
claude --version
```

`2.x.x (Claude Code)` 같은 숫자가 나오면 성공입니다.

> 윈도우에서 `'irm' is not recognized...` 오류가 나면 PowerShell이 아니라 CMD 창입니다.
> 창 왼쪽에 `PS C:\` 가 보이면 PowerShell, `C:\` 만 보이면 CMD입니다.

> 윈도우에서는 [Git for Windows](https://git-scm.com/downloads/win)를 같이 깔아두면 클로드가 쓸 수 있는 도구가 늘어납니다. 필수는 아닙니다.

---

## 3. 첫 실행 (5분)

### ① 작업할 폴더를 하나 만듭니다
예: `문서\수업자료` — 앞으로 만들 학습지·앱이 전부 여기 쌓입니다.

### ② 그 폴더에서 클로드를 켭니다

**쉬운 방법:** 탐색기로 그 폴더를 연 뒤, 주소창에 `powershell` 이라고 치고 엔터 → 그 폴더에서 창이 열립니다. 거기에:

```
claude
```

처음 실행하면 브라우저가 열리며 로그인을 요구합니다. 로그인하면 끝, 다음부터는 안 묻습니다.

### ③ 말을 겁니다

```
이 폴더에 뭐가 있는지 알려줘
```

```
2학년 생명과학 신경계 단원 학습지를 A4 2장짜리로 만들어줘. 빈칸 채우기 위주로.
```

한국어로 그냥 말하면 됩니다. 명령어를 외울 필요 없습니다.

### ④ 알아둘 조작 3개

| 조작 | 뜻 |
|---|---|
| `Esc` | 하던 거 멈춰! (잘못 가고 있을 때 바로 누르세요) |
| `/clear` | 대화 초기화 (다른 주제로 넘어갈 때) |
| `/help` | 쓸 수 있는 명령 목록 |
| `claude -c` | 아까 하던 대화 이어서 하기 |

---

## 4. 처음 시켜보기 좋은 일 5가지

무난하게 성공하는 것부터 시작해서 감을 잡으시길 권합니다.

1. **교과서 PDF → 학습지**
   > "이 PDF에서 그림 뽑아서, 그림에 번호 붙인 학습지 만들어줘. 인쇄하면 A4 딱 맞게."

2. **수행평가 활동지 + 채점 루브릭**
   > "혈액형 판정 실험 수행평가 활동지랑 20점 배점 루브릭 만들어줘."

3. **학생 제출용 웹페이지**
   > "학생들이 폰으로 기말 목표 점수 적어내는 페이지 만들어줘. 내가 결과를 한눈에 보게."

4. **잡무 자동화**
   > "이 폴더 hwp 파일 전부 열어서 학생 이름이랑 활동 내용만 뽑아 표로 정리해줘."

5. **이미 있는 자료 고치기**
   > "지난번 학습지에서 '티록신'을 '타이록신'으로 다 바꿔줘. 2022 개정 용어야."

---

## 5. 곁들여 필요한 프로그램 · 계정

> **먼저 강조: 지금 당장은 아무것도 안 깔아도 됩니다.**
> 클로드 코드만 있으면 학습지·문서·웹페이지 만들기는 다 됩니다.
> 아래는 **"이런 걸 하고 싶어질 때 그때 깔면 되는 것"** 목록입니다. 미리 다 준비하려다 지치는 게 제일 흔한 실패입니다.

### 한눈에 보기

| 언제 필요해지나 | 필요한 것 | 돈 |
|---|---|---|
| 클로드 코드 자체 | **클로드 계정 (Pro 이상)** | 유료 |
| PDF에서 그림 뽑기, hwp 자동 변환, 엑셀 정리 | **파이썬** | 무료 |
| 실수 되돌리기, 자료 백업 | **Git** | 무료 |
| 만든 웹앱을 학생 폰에서 열게 하기 | **GitHub 계정** | 무료 |
| 학생이 제출한 데이터를 서버에 모으기 | **Supabase 계정** (또는 Firebase) | 무료 구간 있음 |
| hwp 파일 자동 변환 | **한글(HWP) 프로그램이 PC에 설치** | 기존 보유 |
| 내가 만든 앱 안에 AI 기능 넣기 | **클로드 API 키 (별도 과금)** | 구독과 별개 유료 |
| 본격적인 웹앱 개발 | **Node.js** | 무료 |

---

### ① 파이썬 (Python) — 제일 먼저 필요해집니다

**뭘 할 때:** PDF 교과서에서 그림 뽑기 / hwp를 PDF로 일괄 변환 / 스캔한 답안지 이미지 처리 / 엑셀·CSV 정리 / 파일 이름 100개 한 번에 바꾸기

**설치:**
- https://www.python.org/downloads/ 에서 최신 버전 다운로드
- ★**설치 첫 화면의 `Add python.exe to PATH` 체크박스를 반드시 켜세요.** 이거 하나 놓쳐서 헤매는 경우가 대부분입니다.

확인:
```
python --version
```

**라이브러리는 직접 안 깔아도 됩니다.** 클로드가 필요한 걸 알아서 설치합니다.
(예: PDF 다루는 PyMuPDF, 한글 파일 다루는 pyhwpx, 엑셀 다루는 openpyxl)

> 로그인 없음. 계정 필요 없습니다.

---

### ② Git + GitHub — 되돌리기 + 무료 배포

둘은 다른 것입니다.
- **Git** = 내 컴퓨터에 깔리는 프로그램. **"자동 저장 + 되돌리기"** 기능.
- **GitHub** = 웹사이트. Git으로 관리한 폴더를 인터넷에 보관·공개하는 곳. **계정 필요.**

**뭘 할 때:**
- 클로드가 파일을 잘못 고쳤을 때 이전 상태로 되돌리기
- 학교 PC ↔ 집 PC 자료 동기화
- 만든 웹앱을 학생이 접속할 수 있는 주소로 올리기 (GitHub Pages, 무료)

**설치·가입:**
1. Git: https://git-scm.com/downloads/win → 설치 (옵션은 전부 기본값으로 넘기면 됩니다)
2. GitHub 계정: https://github.com → Sign up (무료)

**첫 설정 — 클로드에게 시키면 됩니다:**
> "이 폴더 git으로 관리 시작해줘. 내 이름은 ○○○, 이메일은 ○○○@... 로 설정하고, 학생 개인정보 파일은 .gitignore로 제외해줘."

**★★ 여기가 제일 위험한 지점입니다 ★★**

GitHub에 올리는 저장소는 **PUBLIC(전체 공개)** 과 **PRIVATE(나만 보기)** 가 있습니다.
- 만들 때 기본이 PUBLIC인 경우가 있습니다. **학생 명단·성적이 든 폴더를 PUBLIC으로 올리면 전 세계에 공개됩니다.**
- 처음엔 **무조건 PRIVATE**으로 만드세요.
- 한 번 올렸다가 지워도 **기록(히스토리)에 남습니다.** 지운다고 없어지지 않습니다.
- 올리기 전에 반드시 물어보세요:
  > "지금 커밋하려는 파일 중에 학생 실명·학번·연락처·성적이 든 게 있는지 먼저 검사해줘."

---

### ③ GitHub Pages — 학생 폰에서 열리는 주소 만들기

만든 웹페이지(설문·퀴즈·투표 앱 등)를 **무료로 인터넷에 올려** 학생들이 폰으로 접속하게 하는 기능입니다. GitHub 계정만 있으면 됩니다.

> "이 앱을 GitHub Pages로 배포해서 학생들이 접속할 주소 만들어줘."

**주의:** GitHub Pages로 올린 페이지는 **주소만 알면 누구나 볼 수 있습니다.** 학생 이름이 화면에 뜨는 앱이라면 비밀번호나 로그인 장치를 반드시 넣으세요. 그것도 시키면 됩니다.

---

### ④ Supabase — 학생이 제출한 걸 모아야 할 때

만든 페이지가 **여러 학생의 입력을 받아 저장**해야 한다면 데이터 보관소가 필요합니다.
선택지는 크게 둘인데, **새로 시작한다면 Supabase를 권합니다.**

| | 로그인 | 특징 |
|---|---|---|
| **Supabase** ★추천 | 깃허브 또는 이메일 | https://supabase.com — 데이터가 **엑셀처럼 표로** 보임. 화면에서 직접 고치거나 CSV로 내려받기 쉬워 학생 제출물 확인·정리에 편함 |
| Firebase | 구글 계정 | https://firebase.google.com — 실시간 반영이 강해 투표 현황판처럼 즉시 갱신되는 화면에 유리. 대신 표가 아닌 계층 구조라 훑어보기가 덜 직관적 |

둘 다 **무료 구간**으로 한 학급~한 학년 규모는 충분합니다. 둘 중 하나만 고르세요(둘 다 쓰면 관리가 두 배).

설정 방법은 클로드에게 물어보면 화면을 단계별로 안내해 줍니다. 다만 **웹사이트에서 프로젝트 만들고 키 복사하는 건 선생님이 직접** 해야 합니다.

**★ 주의:** 여기 저장되는 건 **실제 학생 데이터**입니다.
- 만들자마자 **접근 규칙(보안 규칙)** 을 반드시 설정하세요. 기본값이 "누구나 읽고 쓰기 가능"인 경우가 있습니다.
  > "지금 이 데이터베이스 규칙이 아무나 읽을 수 있게 돼 있는지 확인하고 안전하게 고쳐줘."
- 학년 끝나면 데이터를 어떻게 할지 미리 정해두세요.

---

### ⑤ 한글(HWP) 파일

학교 자료는 대부분 hwp죠. 두 가지 경우가 있습니다.

- **텍스트만 뽑으면 될 때** → 아무것도 안 깔아도 됩니다. 클로드가 파이썬으로 직접 읽어냅니다.
- **hwp를 PDF로 변환하거나 서식까지 살려야 할 때** → **PC에 한글 프로그램이 설치돼 있어야** 합니다. 클로드가 한글을 자동 조종해서 변환합니다.

---

### ⑥ 클로드 API 키 — 내가 만든 앱에 AI를 넣을 때

**구독료와는 완전히 별개로 돈이 나갑니다.** 헷갈리기 쉬운 부분입니다.

- **클로드 코드 쓰는 것** = Pro 구독으로 커버 (추가 요금 없음)
- **내가 만든 앱이 스스로 AI를 호출하는 것** = 별도 API 키 필요, 쓴 만큼 과금

예: "학생 활동 기록을 넣으면 생기부 초안을 뽑아주는 프로그램"을 만들어 쓰는 경우.

발급: https://console.anthropic.com → 결제수단 등록 → API 키 발급

**★★ API 키는 비밀번호입니다 ★★**
- 키가 든 파일을 **절대 GitHub에 올리지 마세요.** 유출되면 남이 내 카드로 쓰게 됩니다.
- 올리기 전 반드시:
  > "이 폴더에 API 키나 비밀번호가 코드에 박혀 있는지 검사해줘. 있으면 .gitignore 처리해줘."
- 실수로 올렸다면 즉시 콘솔에서 그 키를 **폐기(revoke)** 하고 새로 발급하세요.

---

### ⑦ Node.js — 나중에

일반 웹페이지(HTML 한 장짜리)는 필요 없습니다. 여러 화면이 있는 본격적인 웹앱을 만들 때만 필요합니다. 그때 https://nodejs.org 에서 **LTS** 버전을 받으시면 됩니다. 계정 필요 없습니다.

---

### 계정 정리 (비밀번호 관리)

지금까지 나온 로그인은 이렇습니다:

| 서비스 | 계정 | 언제 |
|---|---|---|
| Claude (claude.com) | 이메일 또는 구글 | 필수 · 유료 |
| GitHub | 이메일 | 백업·배포할 때 |
| Supabase ★ | 깃허브/이메일 | 데이터 모을 때 |
| Firebase | 구글 계정 | 데이터 모을 때 (Supabase 대신) |
| Anthropic Console | 이메일 | 앱에 AI 넣을 때 · 별도 유료 |

**권장:**
- **학교 업무 계정과 개인 계정을 섞지 마세요.** 나중에 인수인계·퇴직 시 문제가 됩니다.
- 결제가 걸린 계정(Claude, Anthropic Console)은 **2단계 인증**을 켜세요.
- 비밀번호·API 키를 클로드와의 대화창에 붙여넣지 마세요. 필요하면 파일에 넣고 "그 파일을 읽어서 쓰라"고 시키면 됩니다.

---

## 6. 잘 시키는 요령

### 구체적으로
- ✗ "학습지 만들어줘"
- ✓ "2학년 생명과학 '사람의 신경계' 학습지. A4 2쪽, 빈칸 채우기 중심, 교과서 그림 활용, 정답지도 같이."

### 단계로 쪼개서
```
1. 먼저 이 PDF에서 그림들 뽑아줘
2. 어떤 그림이 있는지 보여줘
3. 그 중 뇌 단면도로 학습지 A섹션 만들어줘
```

### 먼저 보게 하고 시키기
바로 만들라고 하기 전에 **"먼저 이 폴더 파일들 훑어보고 어떻게 할지 계획만 말해줘"** 라고 하면 훨씬 정확해집니다.

### 마음에 안 들면 그냥 말하면 됩니다
> "표 글씨가 너무 작아. 키우고 셀 안에서 가운데 정렬해줘."

파일을 직접 고쳐줍니다. 처음부터 다시 만들 필요 없습니다.

---

## 7. ★ 학생 개인정보 — 이건 꼭 읽어주세요

가장 크게 사고 날 수 있는 지점입니다.

1. **학생 실명·학번이 든 파일은 인터넷에 올라가는 폴더에 두지 마세요.**
   깃허브(GitHub)에 백업하거나 웹에 배포하는 순간 공개될 수 있습니다.

2. **웹앱을 만들어 배포할 때는 반드시 물어보세요.**
   > "이 앱에서 학생 개인정보가 외부에 노출될 위험이 있는지 점검해줘."

3. **실명 대조표는 별도 폴더에 분리**하고, 공유되는 문서에는 "1번 학생", "A반 3번" 처럼 익명으로 적는 습관을 들이세요.

4. **파일을 지우라고 시키기 전에 한 번 더 확인하세요.** 클로드는 시키면 지웁니다.

---

## 8. 안전장치

### 승인 모드
기본값은 **파일을 고치기 전에 물어봅니다.** 내용을 보고 승인하세요.
`Shift+Tab` 을 누르면 모드가 바뀝니다 —
- **기본**: 매번 승인 (처음엔 이걸로)
- **계획(plan)**: 고치지 않고 계획만 제안
- **자동 승인**: 안 물어보고 진행 (익숙해진 뒤에)

### 백업은 스스로
중요한 자료는 작업 전에 폴더를 복사해 두시거나, 클로드에게
> "이 폴더 깃(git)으로 버전 관리 시작해줘. 개인정보 파일은 제외하고."
라고 시키면 되돌리기가 가능해집니다.

### 결과물은 반드시 검토
학습지 내용, 채점 기준, 교육과정 성취기준은 **선생님이 확인해야 합니다.**
그럴듯하지만 틀린 내용이 섞일 수 있습니다. 특히 교과 용어와 성취기준 코드.

---

## 9. 한 단계 위 — 기억시키기 (CLAUDE.md)

같은 설명을 매번 반복하는 게 귀찮아지면, 작업 폴더에 `CLAUDE.md` 파일을 만들어 두세요.
클로드가 그 폴더에서 작업할 때마다 **자동으로 먼저 읽습니다.**

만드는 법도 말로 시키면 됩니다:

> "CLAUDE.md 만들어줘. 나는 고등학교 생명과학 교사고, 학습지는 항상 A4 기준에 파란색 계열 디자인으로, 2022 개정 교육과정 용어를 쓴다는 걸 기억하게."

여기에 자주 쓰는 디자인 규칙, 담당 과목, 자주 하는 실수 등을 적어두면 결과물 품질이 확 올라갑니다.

---

## 10. 자주 막히는 곳

| 증상 | 해결 |
|---|---|
| `claude`가 인식이 안 됨 | 터미널을 완전히 껐다 켜세요. 그래도 안 되면 `claude doctor` |
| 로그인이 안 됨 | 무료 계정인지 확인 (Pro 이상 필요) |
| 학교 PC에서 설치 실패 | 보안 정책 문제. 개인 노트북 또는 데스크톱 앱으로 시도 |
| 엉뚱한 방향으로 감 | `Esc`로 즉시 중단 → 뭘 원하는지 다시 구체적으로 |
| 대화가 길어져 느려짐 | `/clear` 로 초기화 후 다시 시작 |
| 한글이 깨져 보임 | "결과를 UTF-8 파일로 저장해서 보여줘" 라고 요청 |

---

## 11. 요약

```
1. Pro 이상 클로드 구독            → claude.com/pricing
2. 설치                           → claude.com/download (앱)
                                    또는 PowerShell에 irm https://claude.ai/install.ps1 | iex
3. 작업 폴더 만들고 그 안에서 claude 실행
4. 한국어로 구체적으로 시킨다
5. 결과물은 반드시 검토, 학생 개인정보는 분리
```

**나중에 필요해지면 그때 추가:**

```
PDF·hwp·엑셀 자동 처리   → 파이썬 (설치 시 'Add to PATH' 체크 ★)
되돌리기 · 백업           → Git
학생 폰에서 열리는 주소    → GitHub 계정 (저장소는 PRIVATE부터)
학생 제출 데이터 모으기    → Supabase (또는 Firebase)
내 앱에 AI 넣기           → Anthropic Console API 키 (별도 과금 ★)
```

**절대 금지 3가지**
1. 학생 명단·성적이 든 폴더를 GitHub **PUBLIC**으로 올리기
2. API 키·비밀번호가 든 파일을 GitHub에 올리기
3. 결과물을 검토 없이 그대로 학생에게 배포하기

---

**처음 일주일은 "이미 손으로 하고 있던 일" 하나를 골라 시켜보세요.**
새로운 걸 만들려고 하기보다, 매년 반복하던 잡무 하나가 자동화되는 순간에 감이 옵니다.

---

*공식 문서: https://code.claude.com/docs*
