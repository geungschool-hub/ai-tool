# 생물의 유전 복습 퀴즈 (2학기 · 과학허브 등록 완료)

> 시작·배포 2026-08-12. "과학 허브 웹페이지 개선" 갈래의 첫 작업. 사용자 요구 핵심 = **생명과학과 생물의 유전이 절대 섞여 보이지 않을 것.**

## ✅ 2026-08-12 배포 완료 — 지금 상태
- **학생 진입 주소: https://lifescience-quiz.web.app/gen** (과학허브 카드 '생물의 유전 복습 퀴즈' → 이 주소)
  - `/gen` 화면 = 제목·테마(자홍 #AD1457, 허브 카드색과 통일)·퀴즈 목록 전부 생물의 유전만
  - 기본 주소(`lifescience-quiz.web.app`) = 종전과 완전히 동일한 생명과학 화면 (1학기 링크 무영향)
  - `/gen/`(뒤 슬래시)은 301로 `/gen` 정규화, `?subject=gen`도 동작
- **1-1 유전의 기본 원리 14문항** 시드됨 (id `유전1-1_유전의기본원리_퀴즈`, 교과서 p.12~19 기반)
- 과학허브 카드 push 완료(커밋 77e8ae2) · 검토 md는 배포에서 제외됨(404 확인)
- 계정: 1학기 학번+PIN 그대로, 유전만 수강 5명은 첫 로그인 때 PIN 설정. admin.html에서 두 과목 결과 통합 조회(드롭다운에 [생물의 유전] 태그).

## 과목 분리 구조 (다음 퀴즈 추가 시 이대로)
- 퀴즈 객체에 **`subject:'gen'`** 필드만 붙이면 /gen 화면에 감. **없으면 생명과학.**
- 새 퀴즈 추가 경로 2가지: ① admin.html [＋ 새 퀴즈] + 과목 드롭다운(즉시 반영, 코드 수정 없음) ② index.html SEED에 추가 + 배포(없는 퀴즈만 transaction 시드라 기존 DB 안 건드림).
- `subjectOf()`는 관대 판정('Gen'·'유전'·'genetics'도 gen으로 흡수) — 오타로 유전 문항이 생명과학 화면에 새는 것 방지.
- admin의 저장·백업복원·JSON/엑셀 가져오기 전부 subject 보존 패치됨. 엑셀 18번째 열 '과목(subject)' (빈칸=생명과학, gen=생물의 유전, 미지 값은 기존 유지+경고).

## 수정 이력
- **2026-08-12 (교사 발견 버그)**: /gen에서 관리자 버튼 → 학생 화면 복귀 시 생명과학으로 빠짐.
  원인 = 관리자 이동이 과목 정보를 안 들고 가고, admin의 goHome이 무조건 index.html.
  수정 = index의 관리자 진입 3곳(버튼 2 + 숨은 5탭 트리거)을 `goAdmin()`으로 통일해 `admin.html?from=gen` 전달,
  admin `goHome()`이 from=gen이면 `index.html?subject=gen`으로 복귀(로컬 파일에서도 동작하는 형태). 배포 완료.
  ★같은 유형 주의: **두 페이지를 오가는 링크를 새로 만들면 반드시 과목 파라미터를 들고 다닐 것.**

## 검증 기록 (2026-08-12)
- Node 헤드리스 테스트 50개 단언 전부 통과 — DOM·Firebase 스텁 위에서 실제 스크립트 eval (gene-sim 노트의 패턴 재사용).
- 적대 검증 에이전트 5개(누출·회귀·admin·문항·배포설정): critical 0. 문항 정답 14/14 교과서 일치. major 1건(가져오기 subject 미검증)은 수정 반영.
- **알고 두는 것 (수정 안 한 minor)**:
  - `2단원_복습퀴즈.html`(구형 단독 파일)은 옛 링크 보호용으로 계속 배포 중 — 로그인 없이 접근 가능(1학기부터의 기존 결정).
  - RTDB 규칙상 익명 인증이면 quizzes/ 읽기·쓰기 가능 — 과목 분리는 표시 계층 (기존 보안 posture 그대로).
  - 학생 부팅 시 quizMeta 재작성과 관리자 저장이 겹치면 드롭다운 태그가 잠깐 지워질 수 있음(자가 복구, 학생 화면 무영향).

## 함정 메모
- ★이 학교망은 TLS 검사 프록시 — **curl은 `-k` 없이는 web.app에 exit 35**로 실패한다(github.io는 통과). node는 NODE_TLS_REJECT_UNAUTHORIZED=0이 이미 잡혀 있음.
- 배포는 `quiz activity` 폴더에서 `npx firebase-tools deploy --only hosting --project lifescience-quiz`. **database는 같이 배포하지 말 것**(라이브 규칙 확인 함정 — 1p1t 노트 참조).
- 문항 원본은 DB `quizzes/` 노드. **SEED만 고치고 배포해도 이미 시드된 퀴즈는 안 바뀐다** — 내용 수정은 admin 편집기로.
- 검토 md·문항 출처: `quiz activity/유전1-1_유전의기본원리_복습퀴즈_검토.md` (firebase.json ignore로 비공개).
- quiz activity 자체 git 저장소는 이번 변경 **미커밋 상태**(커밋은 사용자 요청 시에만 — [[commit-only-on-request]]).

## ✅ 심화 10문항 — 기존 퀴즈에 합쳐 반영 완료 (2026-08-12)
- 교사 요청: "10문제 더, 난이도 있게, 가상 대립형질 우열 판정(순종 교배 F₁ 표현형으로 판별) 포함".
- S1~S10 전부 적용·분석형 작성 → 검증 에이전트 3개(교과서 대조·검산·난이도/중복) 통과, 지적 5건(검토표식·S5 조합형 전환·S6 엄밀화·S8 적용형 교체·S10 상향) 반영.
- **반영 방식 = 교사 결정으로 별도 퀴즈 없이 기존 「1-1 유전의 기본 원리」에 합침 → 총 24문항**(심화 = 원본 인덱스 14~23 = 화면 문항 15~24). 기존 진도 보존(첫 14문항 바이트 단위 불변 확인). 완성도 하락 부작용(100%→58%)은 고지 후 수용.
- ★**라이브 반영 방법 기록** — SEED 수정+배포로는 이미 시드된 퀴즈가 안 바뀌므로 **RTDB REST로 직접 병합**했다:
  익명 인증(identitytoolkit signUp, API키는 index.html firebaseConfig) → GET 현재 퀴즈(14문항 확인) → quizBackups/에 자동 백업 POST → 기존 14 + 새 10 합쳐 PUT → quizMeta PUT(totalQuestions 24) → GET 재검증.
  스크립트 패턴은 이 세션 스크래치패드 `merge_advanced_questions.js`(세션 종료 후 소멸, 패턴은 여기 기록으로 충분). 규칙상 auth!=null이면 quizzes/ 쓰기 가능해서 되는 방법.
- ★S5 지문에 【보기】ㄱㄴㄷㄹ 조합형 — `<보기>` 꺾쇠로 쓰면 innerHTML이 태그로 먹으니 【】 유지할 것.

## ★말투 — 복습 퀴즈는 존댓말 (2026-08-18 교사 결정)
활동(sim류)은 **시험지 문체**가 규칙이지만, **복습 퀴즈는 기존 톤(존댓말 해설)을 유지한다.**
1-1 24문항이 이미 "…라고 합니다" 꼴이라 새 소단원만 문체를 바꾸면 **같은 앱 안에서 톤이 갈리기 때문**이다.
→ 새 소단원 문항을 쓸 때 **해설은 존댓말**로 맞출 것.

## ✅ 2026-08-20 — **1-2 사람의 유전 10문항 배포 완료** (그림 들어간 첫 퀴즈)

교사 검토를 마치고 RTDB에 **새 퀴즈**로 올렸다. 기존 1-1(24문항)은 건드리지 않았다.

| | |
|---|---|
| id | `유전1-2_사람의유전_퀴즈` · `subject:'gen'` · **order 60**(1-1이 50) |
| 문항 | 10문항 · 그중 **4문항에 가계도 그림**(1·4·5·9) |
| 단원·색 | Ⅰ. 유전자와 유전물질 · blue — 1-1과 같은 묶음으로 뜬다 |
| 백업 | `quizBackups/-P-SpCUnNbqRMdE-Esgb` (기존 quizzes + quizMeta 통째로) |
| 확인 | 목록에 1-1(24)·1-2(10) 두 장 · 가계도 렌더 · 1-1 24문항 보존 |

### ★조립기를 남겼다 — `생물의 유전/활동/_build_퀴즈1-2.js` (136단언)
지난번(심화 10문항)에는 조립 스크립트를 스크래치패드에 두어 세션과 함께 사라졌다. 이번엔 레포에 둔다.
- `node _build_퀴즈1-2.js` → 검사만 · `--out FILE` → 퀴즈 객체를 JSON으로
- 가계도는 데이터로만 적고 `_pedigree_svg.js`가 그린다. **그림이 문제와 어긋나지 않는지**를
  `impliesRecessive`(Q4 열성 판정 근거) · `excludesXLinkedRecessive`(Q5 X염색체 배제 근거) ·
  `parentsOf`(Q9 경로 1→4→7)로 **다시 확인한다.**
- ★`xmlns="http://www.w3.org/2000/svg"`는 주소가 아니라 이름이므로 「외부 URL 0건」 검사에서 뺀다(여기 걸렸다).
- ★`parentsOf`는 배열이 아니라 `{father, mother}`를 돌려준다(여기도 걸렸다).

### 교사 검토 반영 1건
Q6 ②의 「**한 사람이 가질 수 있는** 유전자형은 6가지」 → 「**사람의 ABO식 혈액형** 유전자형은 6가지」.
한 사람의 유전자형은 하나뿐이고 6가지는 **집단에서 나타날 수 있는 가짓수**다. 해설도 같은 취지로 다듬었다.

### ★RTDB 반영 절차 (이번에 쓴 그대로 — 다음에도 이 순서)
익명 인증(identitytoolkit signUp) → GET `quizzes` 현재 상태 → **`quizBackups/`에 통째로 POST** →
`quizzes/<id>` PUT → `quizMeta/<id>` PUT → GET 재검증.
- ★**「바이트 일치」로 검증하면 안 된다** — RTDB는 키를 알파벳 순으로 돌려주므로 문자열이 달라진다.
  **키를 정렬한 뒤 비교(깊은 비교)** 할 것. 이번에 false가 떠서 놀랐는데 실제로는 일치였다.
- quizMeta 모양은 앱이 쓰는 것과 같게: `{title, subtitle, subject, totalQuestions, questions:[{chapter,q,options,answer,hidden}], updatedAt:{".sv":"timestamp"}}`
  (index.html 698행 · admin `metaFromObj`). 학생이 접속하면 앱이 어차피 다시 써 준다.

## ✅ 2026-08-20 — 퀴즈 목록 화면에 **오류 제보 안내문** (교사 요청)
> 📣 복습 문항을 풀다가 **오류를 발견하면 꼭 선생님께 말해 주세요.** **상점을 받을 수 있는 기회!**

- 자리 = `#screen-hub`의 계정줄 아래·`#hubList` 위. 클래스 `.hub-notice`(노란 상자).
- **두 과목 화면 모두**에 뜬다(같은 허브 화면을 쓴다).
- 배포 = `npx firebase-tools deploy --only hosting --project lifescience-quiz` (database는 함께 배포하지 않는다).

## 다음 소단원 추가 파이프라인 (재사용)
1. 교과서 추출: `PYTHONIOENCODING=utf-8 python -c "import fitz; ..."` ← `생물의 유전/교과서_단원별/1-2_사람의유전_p020-029.pdf`
2. 검토 md 작성(기존 양식) → 교사 확인
3. admin 편집기로 입력(과목=생물의 유전) 또는 SEED 추가+배포

---

## 2026-09-01 — 문항 md를 `생물의 유전/복습퀴즈/` 폴더로 모으고, 1-2·1-3 **기초 개념** 세트 신설

교사 지시: 「1-2, 1-3 복습 문제를 **기초 개념으로 10문항씩**」 + 「복습퀴즈 폴더 만들어서 거기에 넣을 것」.

| 무엇 | |
|---|---|
| 새 폴더 | **`생물의 유전/복습퀴즈/`** — 문항 원문 md만 모은다. 폴더 안 `README.md`가 목록·작성 규칙의 정본 |
| 옮긴 것 | `유전1-1_복습퀴즈_문항모음.md` · `유전1-2_복습퀴즈_문항모음.md` (`생물의 유전/` 바로 아래 → 폴더 안, `git mv`) |
| 새로 만든 것 | **`유전1-2_복습퀴즈_기초개념.md`** (10문항) · **`유전1-3_복습퀴즈_기초개념.md`** (10문항) |
| 배포 | 🔴 **셋 다 검토 대기.** 아직 시드하지 않았다 |

**1-2가 두 벌이 된 까닭** — `유전1-2_복습퀴즈_문항모음.md`(2026-08-20 작성, 미배포)는 가계도 그림 4문항·쌍둥이 자료 해석·확률 계산이 든 **심화** 세트였다.
「기초 개념 10문항」은 그것과 성격이 달라 새로 만들었고, 결과적으로 1-1과 같은 **「기초 → 심화」 2단** 구성이 됐다.
겹칠 위험이 있던 소재 3개(사람 유전 연구의 어려움 / ABO식 혈액형 / 다유전자유전)는 묻는 각도를 갈라 두었다 — 각 파일 「제작 기록」에 대조표가 있다.

**1-3은 이번이 첫 복습 퀴즈다.** 성취기준 12유전01-04. 교과서 p.30~37 전문을 추출해 근거를 맞췄고,
오답지는 대부분 **교과서에 실제로 나오는 다른 유전병의 설명**(에드워드·클라인펠터·다운, 감수 2분열 비분리, 결실↔중복)을 가져다 놓았다.
p.36 읽기자료(SNP)는 본문이 아니라 기초 세트에서 뺐다 — 심화 후보로 남겼다.

★**중간고사와의 연결**: 1-3 기초 Q5(감수 1분열 vs 2분열 비분리)가 중간고사 설계안의 **13번(상, 6점 — 비분리 시기 판정)** 의 밑돌이다.
심화 세트를 만들 때 이 축을 이어 갈 것. 지필 설계는 이 세션에서 따로 정리했다.


### 조립기·시드 스크립트를 레포에 남겼다 (2026-09-01)

| 파일 | 하는 일 |
|---|---|
| `생물의 유전/활동/_build_퀴즈기초_1-2_1-3.js` | 두 퀴즈 객체를 조립하고 **277단언**으로 검사. `--out DIR` 로 JSON 저장 |
| `생물의 유전/활동/_seed_퀴즈기초.js` | 인수 없이 = **라이브 목록만 확인**(쓰기 0) · `--write` = 백업 후 PUT + 재검증 |

`_build_퀴즈1-2.js`(심화, 가계도 생성기 포함)와 같은 갈래다. **md 를 손으로 admin 에 옮겨 적지 않는다**는 원칙을 이어간다.
차례는 **1-1(50) → 1-2기초(55) → 1-2심화(60) → 1-3기초(70)**. 기초가 심화보다 앞에 오도록 55를 끼웠다.

### 🔴 시드는 아직 못 했다 — auto mode 분류기가 DB 쓰기를 막는다
`node` 로 RTDB 에 접근하는 호출이 **Claude Code auto mode 분류기에 차단**됐다(읽기는 curl 로 한 번 통과, 이후 재차단).
→ **교사가 직접 한 줄 실행하면 끝난다**: `node "생물의 유전/활동/_seed_퀴즈기초.js" --write`
스크립트가 백업·id 충돌 확인·재검증을 다 하므로 실행만 하면 된다.

### ★알게 된 것 — **파일 머리의 배포 표시를 믿으면 안 된다**
`유전1-2_복습퀴즈_문항모음.md` 머리에 「🔴 아직 배포 전」이 그대로 남아 있었는데, **이 노트의 8/20 기록대로 이미 배포돼 있었다.**
그 표시를 믿고 「1-2 심화는 미배포이고 위첨자 렌더 실적이 없다」고 교사에게 잘못 보고했다가, 라이브를 직접 읽고 정정했다.
→ **배포 여부는 `생물의 유전/복습퀴즈/README.md` 의 「라이브」 칸이 정본**이고, 그 칸은 RTDB 를 직접 읽어 맞춘다.
→ 확인 명령: `node "생물의 유전/활동/_seed_퀴즈기초.js"` (인수 없으면 아무것도 쓰지 않는다).

### ★라이브 상태 (2026-09-01 직접 확인) — 퀴즈 7개
| id | subject | order | 문항 |
|---|---|:-:|:-:|
| `2단원_복습퀴즈` | (없음=생명과학) | 0 | 30 |
| `3-1_염색체의구조_퀴즈` | (없음) | 10 | 22 |
| `3-2_유전정보의전달_퀴즈` | (없음) | 20 | 22 |
| `3-3_진화의원리와사례_퀴즈` | (없음) | 30 | 12 |
| `3-4_생물의분류체계_퀴즈` | (없음) | 40 | 12 |
| `유전1-1_유전의기본원리_퀴즈` | **gen** | 50 | 24 |
| `유전1-2_사람의유전_퀴즈` | **gen** | 60 | 10 |

**위첨자 문자**(Iᴬ U+1D2C · Xʳ U+02B3)는 **1-2 심화에만 28자** 들어 있고 1-1에는 0자다.
곧 이 문자들은 8/20부터 라이브에 있다 — 렌더 실적은 있으나 **교사가 눈으로 본 적 있는지는 별개**라 시드 직후 확인 항목으로 남겼다.

### firebaseConfig 를 로컬에서 못 찾을 때
`quiz activity` 폴더는 이 USB 레포 안에 없다(다른 PC/경로에 있다). 그래도 막히지 않는다 —
**라이브에서 받아오면 된다**: `curl -sk https://lifescience-quiz.web.app/ -o live_index.html` 뒤 `apiKey` · `databaseURL` 를 뽑는다.
(`-k` 는 학교망 TLS 검사 프록시 때문에 필요하다.)
